<?xml version="1.0" encoding="utf-8"?>
<!--
Copyright (C) 2025 likme
SPDX-License-Identifier: AGPL-3.0-or-later
-->

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:orientation="vertical"
    android:padding="20dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Logo: main menu only -->
    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="12dp"
        android:contentDescription="@string/app_name"
        android:src="@drawable/ic_logo_entrechat"/>

    <!-- Title: proper noun, localized via strings.xml -->
    <TextView
        android:id="@+id/title"
        android:text="@string/main_menu_title"
        android:textAppearance="?attr/textAppearanceTitleLarge"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnIdentity"
        style="?attr/materialButtonOutlinedStyle"
        android:text="@string/main_menu_identity"
        android:layout_marginTop="24dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnContacts"
        style="?attr/materialButtonOutlinedStyle"
        android:text="@string/main_menu_contacts"
        android:layout_marginTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnOptions"
        style="?attr/materialButtonOutlinedStyle"
        android:text="@string/main_menu_options"
        android:layout_marginTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</LinearLayout>
